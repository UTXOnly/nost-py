server {
    listen 80;
    #listen [::]:80;

    server_name nostr.dojotunnel.online;
    server_tokens off;

    location /.well-known/acme-challenge/ {
        allow all;
        root /var/www/certbot;
    }

    location / {
        return 301 nostr.dojotunnel.online$request_uri;
    }
}